#include <gtest/gtest.h>

TEST(Operation, CreateTable)
{
    // db::Database::getInstance().execute(
    //     "create table users ({key, autoincrement} id : int32, {unique} login: "
    //     "string[32], password_hash: bytes[8], is_admin: bool = false);");
}
